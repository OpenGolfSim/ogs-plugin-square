{
  "name": "ogs-plugin-square",
  "version": "0.0.1",
  "description": "Creates an OpenConnect v1 API server to allow connectors to send shot data over TCP",
  "plugin": {
    "title": "Square HE",
    "type": "launch",
    "requiresPairing": "bluetooth"
  }
}